.modal {
  display: none;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.modal-img {
  margin-bottom: 30px;
}

.modal-title {
  font-size: 16px;
  line-height: 24px;
}